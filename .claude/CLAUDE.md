# CLAUDE.md

Personal memory / preferences that follow me across machines. Loaded by Claude Code
at every session start from `~/.claude/CLAUDE.md` (which is a symlink into `~/dotfiles/.claude/`).

Add notes here as they come up — coding preferences, recurring context, things you
want Claude to know every session.
